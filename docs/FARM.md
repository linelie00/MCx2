# FARM — 채널 농장 기획서

디스코드 봇에 들어갈 **채널 농장** 시스템의 설계 문서입니다.
(작성: 2026-09-22 · 상태: 0~3c단계 배포, 3b 구현(배포 전), 4단계부터 기획)

> 채널 하나가 땅 하나. 먼저 `/농장 등록` 한 사람이 주인이 된다.
> 돌투성이 척박한 땅에서 시작해, 물을 주고 · 돌을 깨고 · 배치를 궁리하고 · 날씨를 읽고 ·
> 마을 주문을 채우며 **비옥한 아홉 밭**으로 키워 가는 것이 목표다.

채택한 요소: **A 궁합·윤작 · B 품질·거대 작물 · C 날씨·계절 · F 마을 주문 · G 이웃·품평회 · H 개간 미니게임**

---

## 0. 확정된 결정

| 항목 | 결정 |
|---|---|
| 농장 수 | **한 사람당 1개.** 실수로 등록한 경우를 위해 `/농장 폐농` 을 둔다 (§11) |
| 등록 가능 채널 | **일반 텍스트 채널(`ChannelType.GuildText`)만.** 스레드·포럼·음성·공지 채널은 거절 |
| 작물 아이템 | **기존 장보기 재료 키를 그대로 재사용**하고 작물을 더 추가한다. 가격 악용 방지는 §10 |
| 씨앗 | **심을 때 골드로 산다**(아이템 아님). 칸당 `파는 값 − 보장 이익`(보장 이익은 성장일로 정한다)이라 **수확만 하면 반드시 이득**이고, 비싼 작물이라고 이익이 크게 늘지 않는다. 오늘의 씨앗 가게는 7단계 (§4) |
| 상점 값 | **재료만 사는 값 ×2**, 파는 값은 그대로. 농장 1단계와 함께 적용 (§4) |
| 성장 기간 | **작물마다 다르다.** 기본값은 가격 구간으로 정하고 작물 성격대로 보정하며, 토질이 좋을수록 빨라진다 (§4) |

---

## 1. 땅의 구조

```
채널 = 농장 1개
 └ 밭 3×3 = 9개   (밭마다 작물 한 종류를 정한다)
    └ 칸 3×3 = 9개 (그 작물을 최대 9포기)
```

밭과 칸의 번호는 둘 다 **키패드 배치**를 쓴다. 가운데가 5번이다.

```
1 2 3
4 5 6
7 8 9
```

- 처음에는 **5번 밭만 열려 있고**, 그 밭도 칸의 절반 이상이 돌이다.
- 나머지 밭은 농장 레벨에 따라 열린다 (§8). 열리는 순서는 5 → 2 → 4 → 6 → 8(십자) → 1 → 3 → 7 → 9(모서리)이다.
  십자 방향 밭이 먼저 열리므로, 초반부터 이웃한 밭끼리의 궁합(§5)을 고민할 수 있다.
- 새로 열린 밭은 돌이 많다. 밭을 연 다음 **개간해야 쓸 수 있다.**

### 칸의 상태

| 이모지 | 상태 | 설명 |
|---|---|---|
| 🔒 | 잠김 | 레벨이 부족해 아직 열리지 않은 밭 |
| 🪨 | 돌 | 개간 1번으로 치운다 |
| ⛰️ | 바위 | 개간 미니게임으로 깬다 (§9) |
| 🌼 | 잡초(민들레) | 빈 칸에 가끔 저절로 난다. 뽑으면 `dandelion` 이 나오고, 뽑기 전에는 심을 수 없다 |
| 🟫 | 빈 흙 | 심을 수 있다 |
| 🌱 | 새싹 | 성장 0~49% |
| 🌿 | 자라는 중 | 성장 50~99% |
| (작물 이모지) | 다 자람 | 수확할 수 있다 |
| 🍂 | 시듦 | 물이 부족하다. 성장이 멈추고 품질 상한이 ★ 로 떨어진다 |
| 💀 | 죽음 | 치우면 퇴비 조각이 나온다 |

### 화면 (`/농장 보기`)

```
🏠 #잡담 농장 · 주인 @미하 · Lv.4
🌤️ 맑음 (봄 3일째) · 내일 ☔ 비 · 💧 오늘 물 줌 — @티아

🥕🥕🌱 ┃ 🪨🟫🪨 ┃ 🔒🔒🔒
🥕🌿🥕 ┃ 🟫🥬🥬 ┃ 🔒🔒🔒
🍂🥕🥕 ┃ ⛰️🥬🥬 ┃ 🔒🔒🔒
━━━━━━━╋━━━━━━━╋━━━━━━━
 ...

2 🥕 당근  토질 ★★☆☆☆  1일 남음  🤝 5번과 궁합
5 🥬 상추  토질 ★☆☆☆☆  재수확 2일
[💧 물주기] [🧺 수확] [🌱 심기] [⛏️ 개간] [📜 게시판]
```

- 밭 아래 줄에는 열린 밭만 한 줄씩 적는다: 작물, 토질, 남은 일수, 궁합.
- 버튼을 두어 명령어를 덜 치게 한다.
- `customId` 접두사는 `farm:` 이다. 기존 컴포넌트 라우팅 규약을 따른다.

---

## 2. 명령어

| 명령 | 누가 | 하는 일 |
|---|---|---|
| `/농장 등록` | 누구나 | 주인 없는 텍스트 채널을 내 농장으로 만든다. 이미 농장이 있는 사람은 거절 |
| `/농장 보기 [채널]` | 누구나 | 농장 화면 |
| `/농장 내농장` | 주인 | 어느 채널에서든 내 농장의 요약을 본다. 채널이 지워졌을 때도 쓸 수 있다 |
| `/농장 물주기` | 누구나 | 오늘의 물을 준다 (§3) |
| `/농장 심기 밭 작물` | 주인·일꾼 | 3×3 칸 선택 버튼 → 확인. 확인 창에 씨앗값 합계를 보여주고, 누르면 골드를 낸다 (§4 씨앗) |
| `/농장 수확 [밭]` | 주인·일꾼 | 다 자란 칸을 수확한다. 밭을 비우면 전부 수확 |
| `/농장 개간 밭` | 주인·일꾼 | 돌을 치우고 바위를 깬다 (§9) |
| `/농장 거름 밭 아이템` | 주인·일꾼 | 퇴비나 비료를 준다 |
| `/농장 설비` | 주인 | 설비를 사거나 확인한다 (§6.4) |
| `/농장 날씨` | 누구나 | 오늘과 내일의 날씨, 이번 계절, 제철 작물 |
| `/농장 게시판` | 누구나 | 마을 주문 (§7) |
| `/농장 납품 주문` | 주인 | 주문을 채운다 |
| `/농장 품평회` | 누구나 | 이번 주 순위와 지난주 결과 (§8.3) |
| `/농장 이웃` | 누구나 | 내가 물을 준 농장과 호감도 |
| `/농장 일꾼 추가/빼기 대상` | 주인 | 일꾼 관리 (최대 2명, 레벨로 해금) |
| `/농장 도감` | 누구나 | 내가 키워 본 작물, 최고 품질, 최고 무게 |
| `/농장 폐농` | 주인 | 농장을 없앤다 (§11) |
| `/농장 씨앗가게` | 누구나 | 오늘의 씨앗 가게 (§15 7단계, 나중에 추가) |

서브커맨드는 25개 한도 안에 든다. 명령 구조가 바뀌면 `npm run register` 를 실행해야 한다.

---

## 3. 물과 시듦

### 물주기

- **칸마다 하루(KST) 한 번** 준다. 누르는 쪽은 한 번이다 — 물이 필요한 칸에 한꺼번에 준다.
- **한 포기에 체력 1** 이 든다(2a). 체력이 모자라면 **줄 수 있는 만큼만** 주고, 급한 칸(🍂 시든 칸 → 어제 못 받은 칸 → 밭이 열린 순서)부터 준다. 남은 포기는 다른 사람이 이어서 줄 수 있다.
  - **체력 1은 남긴다.** 물을 주다 쓰러지면(hp 0) 부활의 영약 없이는 아무것도 못 한다.
  - 카지노 판에 앉아 있으면 못 준다. 판이 도는 동안 체력은 판의 장부에 있다(상점·출첵과 같은 까닭).
  - 거둔 작물을 먹으면(`/사용`) 체력이 찬다. **기른 것을 먹고 다시 물을 주는 순환**이 생긴다.
- **누구나 줄 수 있다.** 그날 물을 준 사람들이 화면에 "💧 — @누구 @누구" 로 뜬다.
- 물을 주면 **그 순간 오늘치 성장이 반영된다.** 준 즉시 새싹이 🌿 로 바뀌는 것이 보여야 재미있다.
- 날씨에 따라 달라진다 (§6):
  - ☔ 비와 폭우인 날은 **자동으로 준 것으로 친다.**
  - ☀️ 폭염인 날은 **두 번**(두 번째는 아무나) 줘야 한다. 한 번만 주면 성장이 절반이다.

### 시듦

칸마다 `thirst`(연속으로 물을 못 받은 날수)를 센다. 비가 온 날은 준 것으로 친다.

| thirst | 결과 |
|---|---|
| 1 | 그날은 자라지 않는다 |
| 2 | 🍂 시듦. 성장이 멈춘다. 한 번 시들면 품질 상한이 ★ 이고, 거대 작물 조건에서 빠진다 |
| 4 | 💀 죽음 |

- 시든 작물은 물을 다시 주면 살아나서 성장을 이어간다. 시든 흔적(`scar`)은 남는다.
- 폭염인 날 한 번도 안 주면 `thirst` 가 2 오른다.
- 물 욕심이 많은 작물(쌀 등)은 모든 단계가 하루씩 앞당겨진다.

### 다 자란 뒤 (과숙)

- 다 자란 날로부터 0~2일이 **제철 수확**이다.
- 3~5일은 **과숙**이다. 품질 점수가 −15 된다.
- 6일째부터는 💀 **썩음**이다.
- 도망가는 버섯갓은 다 자란 날 수확하지 않으면 옆 빈 칸으로 도망가고, 빈 칸이 없으면 사라진다.

---

## 4. 성장 기간

### 기본 성장일

기본 성장일은 **기본가 구간**으로 정한 다음, 작물 성격대로 ±1~2일 보정한다.
토질 ★1, 제철, 맑은 날, 물을 빠짐없이 준 경우의 일수이다.

| 기본가 | 기본 성장일 |
|---|---|
| 1~2 | 2일 |
| 3 | 3일 |
| 4~5 | 4일 |
| 6~8 | 5일 |
| 9~14 | 7일 |
| 15~30 | 10일 |
| 31 이상 | 14일 |

### 하루 성장량

작물은 `days` 만큼의 성장 점수가 쌓이면 다 자란다. 물을 받은 날마다 아래만큼 쌓인다.

```
하루 성장 = 1.0 × 토질배율 × 제철배율 × 날씨배율 × (1 + 궁합보정)
```

| 토질 | ★1 | ★2 | ★3 | ★4 | ★5 |
|---|---|---|---|---|---|
| 성장 배율 | 1.00 | 1.10 | 1.20 | 1.35 | 1.50 |

- 예: 수박(7일)은 ★5 토질에서 4.7일, 즉 **5일째**에 다 자란다.
- 제철이 아니면 제철배율이 **0.5** 이다.
- 궁합보정은 −0.3~+0.3 이다 (§5).

### 재수확 작물

토마토, 딸기처럼 **재수확**이 되는 작물은 수확한 뒤 칸이 비지 않는다. `regrow` 일 만큼 다시 자란다.
과수와 다년생 작물은 제철에만 열매를 맺는다.

### 과수

사과, 레몬, 복숭아, 배, 포도는 **밭 하나(9칸)를 통째로 쓰는 나무 한 그루**이다.

- 심을 때 묘목값을 한 번 낸다(아래 "씨앗"). 첫 열매까지 오래 걸린다(10~12일).
- 그 뒤로는 제철마다 `regrow` 일 간격으로 수확한다. 수확량은 **밭 전체에서 3~9개**(토질 ★1 → ★5)이다. 한 칸짜리 작물 9포기와 하루당 이익이 비슷하도록 맞춘 것이다.
- 나무도 목이 마르다. 다만 죽기까지 thirst 7일로 더 오래 버틴다.

### 씨앗 — 심을 때 산다

씨앗은 **아이템이 아니다.** 심는 순간 칸마다 씨앗값을 골드로 낸다.

#### 전제 — 상점의 사는 값과 파는 값을 나눈다 (0단계)

지금은 `price` 하나로 사고판다. 이것을 둘로 나눈다.

| 값 | 식 | 쓰는 곳 |
|---|---|---|
| **파는 값** (플레이어가 상점에 팔 때) | `price` (지금 그대로) | 상점 팔기, 요리 재료 가치(`worthOf`), 던전 드롭 가중치, 프로필 재산, 수확물 가치 |
| **사는 값** (상점이 플레이어에게 팔 때) | 재료: `buy ?? price × 2` · 소비·잡화: `price` 그대로 | 상점 사기만 |

- 파는 값을 건드리지 않으니 **요리 판매가, 던전, 홀덤 상품, 물고기 값은 그대로다.** 오르는 것은 상점에서 사는 값뿐이다.
- 이렇게 하면 **농사가 채소를 가장 싸게 구하는 길**이 된다.
  - 당근을 상점에서 사면 4골드다.
  - 키우면 씨앗값 1골드로 1~3개가 나온다. 요리 재료로 쓰든 팔든 이득이다.
- 상점에서 산 작물을 되팔면 절반만 돌려받는다. §10의 "사서 납품" 같은 우회로가 더 불리해진다.
- **범위는 재료(`kind: '재료'`)만이다.** 회복약·미끼 같은 소비 아이템과 잡화는 그대로 둔다. HP와 낚시 경제를 흔들지 않기 위해서다.
- 특정 재료를 더 올리고 싶으면 명부에 `buy` 를 따로 적는다.
- **적용 시점:** 농장 1단계(MVP)와 같은 PR로 낸다.

#### 씨앗값 — 수확하면 반드시 남고, 비싼 작물이라고 더 남지 않는다

**원칙 셋**

1. **보장 이익은 가격이 아니라 성장일로 정한다.** 오래 기다린 만큼만 더 남는다.
2. **품질 웃돈은 작게 둔다.** 그래서 최소 이익과 기대 이익이 크게 벌어지지 않는다.
3. **비싼 작물은 까다롭다.** 품질이 덜 나오고, 한 칸에서 덜 나온다. 그래서 하루당 이익이 싼 작물과 비슷해진다.

```
보장 이익 m  = clamp(ceil(기본 성장일 / 2), 1, 파는 값 − 1)
씨앗값(칸당) = 파는 값 − m
```

- 칸당 수확량은 **최소 1개**이고, 어떤 보정으로도 줄이지 않는다. 그래서 수확만 하면 **칸당 m 골드는 반드시 남는다.**
- 연작, 제철이 아님, 과숙, 폭우처럼 **품질·속도만 깎는 요소는 이 보장을 깨지 않는다.**
- **손해가 나는 경우는 작물이 죽었을 때뿐이다.** 방치해서 💀 죽거나 썩었을 때, 그리고 폭풍·서리를 설비 없이 맞아 죽었을 때다. 이것이 관리의 긴장감이다.

**작물 등급 — 비쌀수록 까다롭다**

| 등급 | 파는 값 | 품질 점수 보정 | 칸당 수확량 (토질 ★1 → ★5) |
|---|---|---|---|
| 보통 작물 | 8 이하 | 0 | 1 · 1~2 · 2 · 2~3 · 3 |
| 큰 작물 | 9~14 | −5 | 1 · 1 · 1~2 · 2 · 2~3 |
| 귀한 작물 | 15~30 | −10 | 1 · 1 · 1 · 1~2 · 1~2 |
| 귀한 작물 | 31 이상 | −15 | 1 · 1 · 1 · 1~2 · 1~2 |

**품질 웃돈 (★ 변형의 파는 값)**

| 품질 | 보통 | ★ 상품 | ★★ 특품 | ★★★ 명품 |
|---|---|---|---|---|
| 파는 값 | 파는 값 | ×1.2 | ×1.5 | ×2 |

- 소수점은 올린다.
- 예전 초안(×3 / ×6 / ×15)보다 훨씬 작다. 그래서 요리 재료 가치가 부풀지도 않는다.

**계산 결과** (궁합·날씨·비료 보너스 없이, 제철, 물을 빠짐없이 준 경우)

| 작물 | 파는 값 | 성장일 | 씨앗값 | 보장 이익 | ★1 밭 기대 이익 (하루당) | ★5 밭 기대 이익 (하루당) | 상점에서 사면 |
|---|---|---|---|---|---|---|---|
| 감자 | 2 | 2 | 1 | +1 | +1.6 (0.8) | +8.8 (4.4) | 4 |
| 당근 | 2 | 3 | 1 | +1 | +1.6 (0.5) | +8.8 (4.4) | 4 |
| 양배추 | 3 | 3 | 1 | +2 | +2.6 (0.9) | +14.1 (7.0) | 6 |
| 토마토 | 4 | 4 | 2 | +2 | +2.6 (0.7) | +16.9 (5.6) | 8 |
| 늙은 호박 | 8 | 6 | 5 | +3 | +4.2 (0.7) | +32.8 (8.2) | 16 |
| 수박 | 12 | 7 | 8 | +4 | +5.4 (0.8) | +36.5 (7.3) | 24 |
| 향송이 | 25 | 10 | 20 | +5 | +6.7 (0.7) | +31.6 (4.5) | 50 |
| 사프란 | 60 | 14 | 53 | +7 | +9.6 (0.7) | +65.5 (6.6) | 120 |

- **최소 이익 대비 기대 이익**
  - ★1 밭에서 약 1.3~1.6배다. 예전 초안에서는 3배 이상이었다.
  - 사프란은 예전에 보장 +30, 기대 +100이었는데, 이제 보장 +7, 기대 +9.6이다.
- **하루당 이익**
  - ★1 밭에서 모든 작물이 0.5~0.9골드로 거의 같다.
  - ★5 밭에서는 4.4~8.2골드다. 편차는 두 배 안이다.
  - 비싼 작물을 고르는 이유는 한 번에 크게 버는 것이 아니다. **덜 자주 돌봐도 된다는 것, 주문, 품평회, 요리 재료**가 이유가 된다.
- **땅을 키우는 보람은 그대로다.** 같은 칸에서 ★1 대비 ★5가 5~7배 남는다. 수확량과 품질이 함께 오르기 때문이다.
- 재수확 작물은 두 번째 수확부터 씨앗값이 없다. 그래서 하루당 이익이 조금 더 높다. 대신 밭을 오래 묶어 둬서 윤작을 할 수 없다.
- 표의 숫자는 계산식으로 낸 것이다. 실제 플레이 흐름(물을 빼먹는 날, 날씨)은 `simulate-farm` 으로 다시 확인한다.

**심기 판정 (`POST /farms/plant`)**

서버가 아래를 한 번에 확인한다.

1. 작물이 농장 레벨에서 해금됐는가
2. 고른 칸이 모두 빈 흙(🟫)인가, 그 밭에 다른 작물이 있지 않은가
3. 골드가 `씨앗값 × 칸 수` 이상인가 (계정의 −1000 하한 규칙을 그대로 따른다)

조건이 맞으면 **골드를 빼고 칸을 🌱 로 바꾸는 것을 한 번에** 한다. 하나라도 어긋나면 아무것도 바꾸지 않는다.

**예외**

| 종류 | 사는 방법 |
|---|---|
| 과수 묘목 | 밭 하나에 묘목값 `파는 값 × 2` 를 한 번 낸다 (레몬·포도 8, 사과 10, 복숭아 12, 배 14). 칸당 씨앗값은 없다. 첫 수확이 최소 3개라 **첫 수확에서 이미 이득**이고, 그 뒤 수확은 전부 이익이다. 첫 열매까지 10~12일을 기다려야 하므로 묘목값은 싸게 둔다 |
| 희귀 씨앗 | 개간과 주문 보상으로만 얻는다. 계정의 씨앗 주머니(`pouches`)에 들어가고, 심을 때 칸마다 1개씩 쓴다. 골드는 들지 않는다 |

씨앗을 아이템으로 두지 않는 이유는 둘이다.

- 사서 되팔거나, 수확물을 씨앗으로 써서 불리는 길이 원천적으로 없다 (§10).
- 작물마다 씨앗 키를 만들 필요가 없다.

> 나중 단계에서 **오늘의 씨앗 가게**(§15 7단계)를 얹는다. 이것도 씨앗 아이템을 만들지 않는 원칙을 지킨다.

---

## 5. A — 궁합과 윤작

### 계열

모든 작물은 계열이 하나씩 있다.

| 계열 | 작물 예 |
|---|---|
| 뿌리 | 감자, 당근, 무, 고구마, 비트, 순무, 토란, 생강, 인삼 |
| 파속 | 양파, 마늘, 대파 |
| 잎 | 상추, 시금치, 양배추, 배추, 브로콜리 |
| 열매 | 토마토, 가지, 고추, 파프리카, 용의 고추 |
| 박 | 오이, 호박, 수박, 참외, 멜론 |
| 콩 | 콩, 완두콩, 강낭콩 |
| 곡물 | 밀, 귀리, 보리, 쌀, 메밀, 옥수수, 참깨, 해바라기, 황금 밀 |
| 허브 | 깻잎, 박하, 로즈마리, 바질, 라벤더, 사프란, 월광초 |
| 베리 | 딸기, 블루베리, 산딸기, 파수꾼 베리 |
| 과수 | 사과, 레몬, 복숭아, 배, 포도 |
| 버섯 | 향송이 |
| 괴식 | 비명 뿌리, 도망가는 버섯갓 |

### 이웃 밭 궁합

**상하좌우로 맞닿은 밭**끼리만 영향을 준다. 대각선은 영향이 없다.

| 조합 | 효과 |
|---|---|
| 콩 ↔ 아무거나 | 이웃 밭이 수확할 때 토질 경험 +50% |
| 콩 ↔ 곡물·잎 | 성장 +10% |
| 파속 ↔ 뿌리(당근 등) | 성장 +10%, 품질 +5 (벌레를 쫓는다) |
| 파속 ↔ 콩 | 성장 −10% |
| 허브 ↔ 열매·잎 | 품질 +5 |
| 박 ↔ 박 | 성장 −10% (덩굴끼리 다툼) |
| 열매 ↔ 뿌리 | 성장 −10% |
| 과수·옥수수·해바라기 ↔ 이웃 | 그늘이 진다. 버섯·잎은 +10%, 열매·박은 −5% |
| 버섯 | **그늘 이웃이 없으면** 성장 ×0.5 |
| 괴식(비명 뿌리) ↔ 이웃 | 성장 −10% (시끄러움) |

- **세 자매 보너스:** 곡물(옥수수), 콩, 박 세 밭이 서로 이어져 있으면(ㄱ자나 일자) 셋 모두 성장 +20%, 품질 +10.
- 한 밭에 걸린 궁합보정은 **합쳐서 ±30%** 에서 자른다.
- 화면에서는 🤝(좋음)과 ⚔️(나쁨)로 표시한다.

### 윤작과 연작

밭마다 최근 수확한 계열 3개(`history`)를 기억한다.

| 상황 | 효과 |
|---|---|
| 직전과 **같은 계열** (연작) | 성장 −15%, 품질 −10, 토질 경험 0 |
| 최근 3번이 **모두 다른 계열** (윤작) | 품질 +10, 토질 경험 +50% |
| 콩 계열을 수확함 | 토질 경험 +10 추가 (질소 고정) |

과수와 다년생 작물은 연작과 윤작을 따지지 않는다.

### 3a 구현에서 정한 것

- 규칙은 `server/src/farm/affinity.js` 한곳에 있다. 봇은 서버가 준 `mods`(보기)와 미리보기(`GET /:channelId/preview`)만 그린다.
- **궁합 ±30% 상한**은 이웃 궁합과 세 자매를 더한 몫에만 건다. 연작(×0.85)과 그늘 없는 버섯(×0.5)은 상한 밖에서 따로 곱한다.
- **세 자매**는 옥수수·콩·박 세 밭 가운데 두 쌍이 맞닿으면(ㄱ자·일자) 셋 모두 받는다.
- **`history` 는 밭이 비워질 때** 그 작물 계열을 적는다(최근 셋). "비워짐" 은 작물 칸(자라는 칸·치우기 전 죽은 칸)이 하나도 없는 것이다 — 돌·잡초는 남아도 된다.
- **이어 심기 연작:** 재수확 작물이 아닌데 밭을 비우지 않고 한 칸씩 이어 심어 **아홉 칸 넘게** 거두면(`streak`) 연작으로 본다. 칸 하나를 늘 남겨 두는 것으로 연작을 피하지 못하게 한 것이다. 연작(⚔️)이 뜨면 **더 심지 말고 밭을 비워** 작물을 바꾸는 것이 요령이다.
- 토질 경험 배율은 곱한다: 연작 ×0, 윤작 ×1.5, 콩 이웃 ×1.5(둘 다면 ×2.25).
- 품질 보정(허브 +5, 세 자매 +10, 윤작 +10, 연작 −10 …)은 셈해 두기만 하고 **3b 가 읽는다.**
- `simulate-farm`(60일): 궁합 미리보기를 보고 고르고 연작 밭은 비우는 플레이 — 수익 3,848골드 · 토질 ★2.90. 늘 같은 작물을 이어 심는 플레이 — 3,281골드 · ★2.56. Lv10 은 둘 다 47~51일.
- 3a 를 만들다 **2a 의 버그**를 찾았다: 돌 하나만 남아 있어도 다 거둔 밭이 작물에 묶여 다른 작물을 못 심었다. 고쳤고, 이미 묶인 밭은 읽을 때 푼다.

---

## 6. C — 날씨와 계절

### 6.1 계절

- **1주(7일)에 한 계절**이 바뀌고, 4주가 1년이다: 봄 → 여름 → 가을 → 겨울.
- 계산은 KST 날짜로 `floor(epochDay / 7) % 4` 이다. 모든 농장이 같은 계절을 쓴다.
- 작물마다 제철이 있다(§12 표). 제철이 아니면:
  - 성장 ×0.5
  - 품질 −20
  - 겨울에는 제철 표시가 없는 작물이 서리에 약하다

### 6.2 날씨

- 날씨는 **KST 날짜의 해시로 정한다.** 모든 농장이 같은 날씨이고, `pickOfDay` 와 같은 방식이다.
- 정해진 값이라 **내일 날씨를 미리 알 수 있다.** `/농장 날씨` 가 오늘과 내일을 보여준다.
  - 예: "내일 비가 오니 오늘 심어 두자", "내일 서리니 덮개를 사자"

| 날씨 | 봄 | 여름 | 가을 | 겨울 | 효과 |
|---|---|---|---|---|---|
| 🌤️ 맑음 | 40 | 35 | 45 | 35 | 기본 |
| ☁️ 흐림 | 20 | 15 | 20 | 25 | 성장 ×0.9 |
| ☔ 비 | 25 | 15 | 20 | 10 | 자동 물주기 |
| ⛈️ 폭우 | 5 | 10 | 5 | 0 | 자동 물주기, 뿌리 계열 품질 −10 (배수로가 있으면 막는다) |
| ☀️ 폭염 | 0 | 20 | 0 | 0 | 물 2번 필요 (빗물통이 있으면 두 번째를 대신한다) |
| ❄️ 서리 | 0 | 0 | 5 | 30 | 제철이 아닌 칸이 thirst +1처럼 상한다 (덮개가 있으면 막는다) |
| 🌪️ 폭풍 | 5 | 5 | 5 | 0 | 키 큰 작물(옥수수, 해바라기) 칸의 30%가 쓰러져 🍂 된다 (지지대가 있으면 막는다) |
| 🌈 무지개 | 5 | 0 | 0 | 0 | 비 온 다음 날에만. 오늘 수확하는 작물 품질 +10 |

※ 확률은 계절별 %이다. 봄에 폭우와 폭풍이 같이 나오지 않게 하는 등, 표의 합은 구현하면서 100으로 맞춘다.

### 6.3 날씨를 타는 작물

| 작물 | 특징 |
|---|---|
| 월광초 | 🌤️ 맑은 날에만 자란다 |
| 용의 고추 | ☀️ 폭염인 날 성장 ×2 |
| 쌀 | ☔ 비 오는 날 성장 ×1.2. 물 욕심이 많다 |

### 6.4 설비 (골드 소모처)

| 설비 | 해금 Lv | 값 | 효과 |
|---|---|---|---|
| 빗물통 | 4 | 600 | 폭염 날 두 번째 물을 대신한다 |
| 덮개 | 4 | 밭당 150 | 서리를 막는다 (밭 단위) |
| 배수로 | 6 | 800 | 폭우 피해를 막는다 |
| 지지대 | 6 | 밭당 200 | 폭풍 피해를 막는다 (밭 단위) |
| 스프링클러 | 9 | 3000 | **주 1회** 물을 못 준 날을 준 것으로 친다. 매일 물주기를 없애지는 않는다 |

- 가격은 초안이다. 시뮬레이션(§13)으로 맞춘다.
- 설비는 계정 아이템이 아니다. **농장 문서의 필드**로 저장한다.

---

## 7. F — 마을 주문 게시판

### 두 종류의 주문

| 종류 | 내용 |
|---|---|
| 📜 마을 주문 | 날마다 3건, **모든 농장이 함께 보는** 게시판이다. 먼저 납품한 농장이 가져간다. 채널끼리 경쟁하게 된다 |
| ✉️ 개인 의뢰 | 농장마다 하루 1건. 동시에 3건까지 쌓인다 |

### 주문 내용

```js
{ id, from: 'npc:migel', crop: 'carrot', qty: 6, minStar: 1,
  reward: { gold: 180, farmXp: 15, seed?: 'goldenWheat', mt?: 1 },
  postedDay, dueDay }
```

- `minStar` 는 1 이상이다. **상점에서 산 보통 작물로는 채울 수 없다** (§10).
- **기한 = 그 작물의 성장일 + 2일.** 게시판을 보고 심어도 맞출 수 있어야 한다.
- 주문 작물은 **이번 계절이나 다음 계절 제철 작물** 가운데, 그 농장 레벨에서 심을 수 있는 것만 나온다.
- 보상 골드 = 작물 기본가 × 수량 × 품질배수 × 1.5 정도.
- 희귀 씨앗과 MT는 드물게 나온다. MT는 **계정당 주 1개**가 상한이다.
- 주문 문구는 NPC 웹훅(미겔, 마티암)의 말투로 보여준다. 예: "마티암: 이번 주 스튜에 쓸 윤나는 당근 여섯 개만…"
- 생성은 `(dayKey, 채널)` 을 시드로 한 정해진 난수로 한다. 서버가 저장하고 판정한다.

---

## 8. 성장감 — 토질, 레벨, 이웃, 품평회

### 8.1 토질 (밭마다 ★1~5)

밭마다 토질 경험(`soilXp`)을 쌓는다.

| ★ | 필요 경험 | 성장 | 칸당 수확량 | 품질 점수 |
|---|---|---|---|---|
| 1 | 0 | ×1.00 | 1 (보통 작물 기준) | +8 |
| 2 | 20 | ×1.10 | 1~2 | +16 |
| 3 | 60 | ×1.20 | 2 | +24 |
| 4 | 140 | ×1.35 | 2~3 | +32 |
| 5 | 300 | ×1.50 | 3 | +40 |

토질 경험을 얻는 방법:

| 방법 | 경험 |
|---|---|
| 수확 | 칸당 +1 |
| 퇴비 | +5 |
| 비료 | +15 |
| 콩 계열 수확 | +10 |
| 윤작 | +50% |
| 작물 죽이기 | −3 |
| 연작 | 0 |

#### 거름 아이템

| key | 이름 | 얻는 법 | 효과 | 밭마다 하루 |
|---|---|---|---|---|
| `compost` | 퇴비 | 죽은 칸·잡초를 치우면 조각이 나오고, 3개면 1개(수확·개간 때 저절로). **거둔 작물 5개 → 1개**(`/농장 퇴비`) | 토질 경험 +5 | 3개 |
| `fertilizer` | 비료 | 상점 🌾 농사 진열대 **30골드**(되팔 수 없음) | 토질 경험 +15 | 1개 |
| `goldFertilizer` | 황금 비료 | 주문 보상(5단계) | 지금 자라는 작물 품질 +15 | — |

- **하루 한도**가 있는 까닭: 없으면 골드만으로 단숨에 ★5 가 된다. **★5 밭에는 안 넣는다**(`soilMax`) — 넘는 경험은 쓰이지 않는다.
- **비료 값은 `simulate-farm` 으로 맞췄다**(이웃+먹으며, 밭마다 하루 한 포대, 골드 200 남기고):

  | 비료 값 | 90일 순수익 | 비료 없을 때와 차이 | 비고 |
  |---|---|---|---|
  | 60 | 10,838 | −2,870 | 초안(200)은 물론 60 도 본전을 못 뽑는다 |
  | **30** | 15,442 | **+1,734** | 60일엔 −581 이지만 밭 아홉이 전부 ★5. **먼저 투자하고 나중에 거두는** 물건 |
  | 20 | 17,211 | +3,503 | 너무 싸다 — 안 쓸 이유가 없다 |

- 퇴비는 **팔 작물을 토질로 바꾸는 선택**이다. 작물을 전부 퇴비로 돌리면 60일에 토질 ★4.3(★5 밭 5.6개)까지 가지만 판 돈이 없다. 팔지 거름으로 쓸지를 고르게 된다.

### 8.2 농장 레벨 (Lv 1~10)

#### 경험치 (farmXp)

| 행동 | 경험치 |
|---|---|
| 수확 | 칸당 1(재수확으로 거둔 칸은 0.5). **품질 경험치는 없다**(3b — 시뮬레이션으로 0) |
| 새 작물 첫 수확 | +10 |
| 거대 작물 | +30 |
| 돌 개간 | 1 |
| 바위 개간 | 3 (완벽하게 깨면 +2) |
| 주문 완수 | 10~40 |
| 그날 첫 물(누가 줬든) | 2 / 일 |

#### 레벨표

| Lv | 누적 XP | 간판 | 해금 |
|---|---|---|---|
| 1 | 0 | 🛖 | 5번 밭 · 1단계 작물 · 나무 곡괭이 |
| 2 | 20 | 🛖 | 2번 밭 · 2단계 작물 · 퇴비 |
| 3 | 50 | 🏠 | 4번 밭 · 3단계 작물 · 비료 · 개간 기력 +1 |
| 4 | 110 | 🏠 | 6번 밭 · 4단계 작물 · 빗물통, 덮개 |
| 5 | 180 | 🏠 | 8번 밭 · 과수 묘목 · 일꾼 1명 |
| 6 | 280 | 🏡 | 1번 밭 · 배수로, 지지대 · 철 곡괭이 |
| 7 | 420 | 🏡 | 3번 밭 · 대형 박류 · **거대 작물** |
| 8 | 600 | 🏡 | 7번 밭 · 일꾼 2명 · 귀한 향신료 |
| 9 | 810 | 🏯 | 9번 밭 · 스프링클러 · 미스릴 곡괭이 |
| 10 | 1050 | 🏰 | 환상 작물 · 칭호 「대농」 |

- 목표 속도는 **매일 성실히 하면 6~8주에 Lv10** 이다. 2a에서 `bot/scripts/simulate-farm.mjs`(서버 규칙 그대로, 70일 × 12판 중앙값)로 맞췄다. 초안(…, 3000)의 0.35배다.

  | 플레이 | Lv2 | Lv3 | Lv5 | Lv10 |
  |---|---|---|---|---|
  | 혼자, 먹지 않음 | 4일 | 6일 | 12일 | 70일 안에 못 감(Lv9) |
  | 거둔 작물을 먹으며 | 4일 | 6일 | 12일 | 47일 |
  | 이웃이 하루 체력 20만큼 도움 | 4일 | 6일 | 12일 | 52일 |
  | 둘 다 | 4일 | 6일 | 12일 | 41일 |

  물 한 포기에 체력 1이라 **체력이 속도의 천장**이다. 먹거나 이웃과 함께 하면 목표 속도가 된다.
- 물주기 경험치는 초안의 "주인이 물주기" 에서 **"그날 첫 물(누가 줬든)"** 로 바꿨다. 주인만 받게 하면 이웃이 먼저 물을 줄수록 주인이 손해를 봤다(시뮬레이션이 잡았다).
- 레벨이 오르면 채널에 축하 임베드를 띄운다. 간판 이모지가 농장 이름 앞에 붙는다.

### 8.3 G — 이웃, 일꾼, 품평회

#### 이웃 물주기 보상

- 남의 농장에 **그날 처음** 물을 주면 보상을 받는다: `10 + 농장Lv × 2` 골드.
  - 폭염인 날 두 번째 물을 준 사람도 받는다.
- 보상을 받을 수 있는 건 **계정당 하루 3곳**까지이다. 자기 농장은 해당하지 않는다.

#### 호감도

(물 준 사람, 농장) 쌍마다 물 준 날수를 쌓는다.

| 단계 | 누적 일수 | 혜택 |
|---|---|---|
| 아는 사이 | 5 | 농장 화면에 "이웃" 으로 이름이 올라간다 |
| 이웃 | 15 | 주인이 수확할 때 이웃에게 **나눔 작물 1개**가 간다. 게임이 새로 만든 것이라 주인 몫이 줄지 않는다. 하루 1회 |
| 단짝 이웃 | 40 | 그 사람이 물을 준 날 성장 +5% ("정성") |

#### 일꾼

- 주인이 지정한다. Lv5에 1명, Lv8에 2명이다.
- 심기, 수확, 개간, 거름을 대신할 수 있다.
- 수확물과 경험치는 **주인에게** 간다. 개간 기력은 **일꾼 자신의 것**을 쓴다.
- 대신 개간해서 나온 전리품은 일꾼이 가진다.

#### 주간 품평회 (KST 월요일 0시에 한 주가 바뀐다)

| 부문 | 기준 |
|---|---|
| 🏋️ 가장 무거운 작물 | 거대 작물과 ★★ 이상 수확에만 무게를 굴린다. 물고기 길이처럼 두 번 굴려 작은 쪽을 쓴다 |
| ⭐ 특품왕 | 그 주에 ★★ 이상을 가장 많이 수확한 농장 |
| 📜 주문왕 | 그 주에 주문을 가장 많이 완수한 농장 |

- 크론 없이 정산한다. **새 주에 처음 들어온 요청**이 지난주를 정산하고 결과를 저장한다.
- 부문별 1위에게는 상금(초안 500골드)을 주고 `fairWins` 를 올린다. 칭호 「품평회 우승」으로 이어진다.
- 결과는 `/농장 품평회` 에서 본다. 정산을 일으킨 명령이 실행된 채널에도 한 번 알린다.

---

## 9. H — 개간 미니게임

### 개간 기력

- 계정당 하루 **5 + 레벨 보너스 + 곡괭이 보너스**이다.
- 돌은 1, 바위는 한 번 휘두를 때마다 1을 쓴다.

### 🪨 돌

1번 휘두르면 치워진다. 미니게임은 없다. 여러 개를 한 번에 치우는 버튼도 둔다.

### ⛰️ 바위 — 결 찾기

- 바위에는 숨겨진 **결** 자리가 하나 있다(1~5).
- 버튼 `[1] [2] [3] [4] [5]` 중 하나를 골라 휘두른다. 기회는 3번이다.
- 빗나가면 힌트가 나온다. 낚시의 거리 힌트와 같은 결이다.
  - "결이 **오른쪽**으로 느껴진다" / "**바로 옆**이다"
- 결과:

| 결과 | 효과 |
|---|---|
| 첫 번째에 맞힘 | ✨ **완벽.** 전리품을 좋은 표에서 2번 뽑는다. XP +2 |
| 두세 번째에 맞힘 | 보통 |
| 3번 다 빗나감 | **금 간 바위**가 된다. 다음에는 결 자리가 공개되고 한 번에 깨진다 |

- 결 자리는 서버가 바위마다 정해 둔다. 봇에는 힌트만 보낸다.

### 곡괭이

| 곡괭이 | 값 | 효과 |
|---|---|---|
| 🪓 나무 (기본) | — | 빗나가면 방향 · "바로 옆" 만 |
| ⛏️ 철 (Lv6) | **300** + `ironLump` ×2 | 빗나가면 결까지 **몇 칸**인지 알려 준다 |
| 💎 미스릴 (Lv9) | **1000** + 원석 ×3(여섯 가지 아무거나, 많이 가진 것부터) | 휘두르기 전에 결 후보를 **두 자리**로 좁혀 준다 — 완벽 확률 20% → 50% |

- **2b에서 초안을 바꿨다.** 시뮬레이션에서 돌은 기력보다 먼저 바닥났다(30일에 남은 돌 0~3개). 그래서 "기력 +2 · 기회 +1" 은 쓸모가 없고, 값(800 / 3000)은 60일 수입의 절반이었다. 곡괭이는 **더 잘 깨서 더 좋은 전리품**을 얻는 물건이 됐다.
- 미스릴의 후보 두 자리는 해시로 고정한다(창을 다시 열어도 같다). **주인 자신의 조회에만** 준다.
- 곡괭이는 **계정 기준**(`tools`, farms.json)이라 폐농해도 남는다. 해금은 자기 농장 레벨로 본다.
- **돌이 떨어지면 개간할 것이 없어진다.** 이것은 나중에 **밭 확장**(3×3 바깥으로 땅을 넓힘)으로 푼다 — 새 땅은 돌투성이로 온다. 2b 기획의 "수확한 칸에 돌이 다시 솟음" 안은 넣지 않았다.

### 전리품

전부 기존 아이템 키를 쓰고, 새로 만드는 것은 `oldCoin` 하나이다.

| 아이템 | 보통 | 완벽 |
|---|---|---|
| 파삭돌 `crackleStone` | 40% | 25% |
| 나뭇가지 `twig` | 15% | 10% |
| 꿈틀지렁이 `earthworm` | 15% | 15% |
| 부러진 화살촉 `brokenArrowhead` | 10% | 10% |
| 원석 `ore*` | 8% | 15% |
| 묵직한 철 덩어리 `ironLump` | 5% | 8% |
| 옛 동전 `oldCoin` (신규, 50) | 4% | 8% |
| 희귀 씨앗 (비명 뿌리, 도망가는 버섯갓, 파수꾼 베리) — **3단계에서 넣는다.** 특수 규칙 없이는 심을 수 없어 2a에서는 화석 몫으로 돌렸다(화석 3% · 9%) | 2% | 5% |
| 정체 모를 화석 `oddFossil` | 1% | 4% |

- 희귀 씨앗과 화석은 **계정당 하루 1개**가 상한이다.

### 잡초

- 빈 칸(🟫)에 하루 5% 확률로 🌼 이 난다.
- 뽑는 데 기력은 들지 않고, `dandelion` 1개와 퇴비 조각이 나온다.
- 잡초가 있는 밭은 성장 −5% 이다.

---

## 10. 가격 악용 방지

### 현재 구조의 전제

- 지금은 `price` 하나로 **사고 판다**(사는 값 = 파는 값). 농장 0단계에서 **사는 값을 `price × 2` 로 나눈다** (§4 씨앗).
- 파는 값은 그대로 `price` 이다.
- 당근처럼 **상점에서 파는 재료를 수확물로 재사용**한다. 그래서 막아야 할 구멍이 넷이다.

| 구멍 | 방지책 |
|---|---|
| ① 상점에서 산 작물을 "내가 키운 것" 으로 납품하거나 품평회에 낸다 | **보통 품질만 기존 키**이다. ★ 이상은 **새 키**(`carrotS1`~`S3`)이고 `shop: false, loot: false` 라 살 수 없다. 주문(`minStar ≥ 1`)과 품평회는 ★ 이상만 받는다 |
| ② 작물을 사서 씨앗으로 심는다 (당근 1개 2골드로 무한히 증식) | **수확물을 씨앗으로 쓰지 않는다.** 씨앗은 아이템이 아니라 심을 때 내는 골드이다(칸당 `파는 값 − 보장 이익`, §4). 희귀 씨앗만 계정의 씨앗 주머니에 들어가고, 되팔 수 없다 |
| ③ 씨앗값보다 수확물이 비싸서 골드가 무한히 생긴다 | 이익은 **시간과 칸 수에 묶여** 있다(최대 81칸, 성장일). 자본을 넣어 규모를 키울 수 없다. 수확하면 반드시 조금은 남고(칸당 보장 이익 1~7골드), 큰 이익은 토질, 품질, 주문에서 나온다. 상점에서 산 작물은 사는 값이 두 배라, 되팔거나 납품해도 손해다. 목표 수익은 §13에서 시뮬레이션으로 맞춘다 |
| ④ 폐농하고 재등록해서 돌을 다시 캔다 (전리품, 초기 보너스) | 폐농 후 **재등록 7일 쿨다운**. 개간 기력과 희귀 전리품 상한은 **농장이 아니라 계정에** 건다 |

### 그 밖의 방지책

- **★ 품질의 값:** 파는 값 ×1.2 / ×1.5 / ×2 이다(올림). 예: 수박 12 → 15 / 18 / 24. 웃돈이 작아서 요리 재료 가치가 크게 부풀지 않는다.
- **거대 작물:** 새 키(`giantPumpkin` 등)이고 `shop: false` 이다. 값은 파는 값 × 45 정도이다. ★5 밭에서 9칸을 평소대로 수확했을 때의 기대 가치(약 ×38)보다 조금 높게 잡아서, 거대 작물을 노리는 것이 손해가 되지 않게 한다.
- **물주기 보상 담합:** 계정당 하루 3곳, 농장당 그날 첫 사람만 받는다. 1인 1농장이라 부계정을 써도 이득이 작다.
- **모든 판정은 서버에서 한다.** 성장, 품질, 무게, 결 위치, 주문 판정 모두 그렇다. 봇은 결과만 받아 그린다.
- **MT:** 농장으로 얻는 MT는 계정당 주 1개가 상한이다.

---

## 11. 등록과 폐농

### 등록 (`/농장 등록`)

1. 채널이 `GuildText` 가 아니면 거절한다. 스레드 안에서 실행하면 "스레드 말고 채널에서" 라고 안내한다.
2. 서버의 `POST /farms/register` 가 **확인과 쓰기를 한 번에** 한다.
   - 이미 주인이 있는 채널이면 거절한다 → 먼저 등록한 사람이 주인.
   - 이미 농장이 있는 사람이면 거절한다 → "내 농장은 #○○ 에 있어요".
   - 폐농 쿨다운 중이면 거절한다.
3. 농장을 만든다.
   - 5번 밭에는 빈 칸 3개, 돌 4개, 바위 2개를 둔다. 첫날부터 심을 수 있게 한다.
   - 나머지 밭에는 돌 3~5개, 바위 2~3개를 무작위로 둔다.

### 폐농 (`/농장 폐농`)

- 채널과 상관없이 **내 농장**을 대상으로 한다. 채널이 지워졌어도 폐농할 수 있다.
- 확인 창은 본인에게만 보인다. 잃는 것을 요약해서 보여준다.
  - 예: "Lv.4 · 밭 4개 · 심긴 작물 23포기 · 설비 2개가 모두 사라져요"
- 버튼은 `[정말 폐농]`(Danger)과 `[취소]` 이고, 30초 뒤 만료된다.
- **등록 후 24시간 안에 폐농하면 '무르기'** 로 보고 쿨다운이 없다. 실수로 등록한 경우를 위한 것이다.
- 그 뒤에 폐농하면 재등록까지 **7일 쿨다운**이 있다.
- 희귀 씨앗 주머니는 농장이 아니라 **계정 쪽에** 둔다(`pouches[userId]`). 폐농해도 남는다.

---

## 12. 작물표

- 표기: **(신규)** 는 새 아이템 키이다(`shop: false, loot: false, sell: true`). 나머지는 기존 키를 재사용한다.
- 성장일은 §4 규칙대로 기본가 구간에 보정을 더한 값이다. `재` 는 재수확 간격이다.
- 제철 표기: 봄 · 여 · 가 · 겨.

### 3c 구현에서 정한 것 — 원본은 `server/src/farm/crops.js`

- **들어간 작물**: 박하(Lv4) · 쌀 · 해바라기(Lv5) · 아스파라거스 · 찻잎 · 생강 · 라벤더 · 참외(Lv6) · 수박 · 멜론(Lv7) · 향송이 · 사프란(Lv8), 희귀 셋(비명 뿌리 · 도망가는 버섯갓 · 파수꾼 베리). 모두 51종.
- **미룬 작물**: 과수 다섯(4단계 — 제철마다 열매, 한 그루가 밭 하나), 용의 고추 · 월광초(4단계 날씨), 인삼(6단계 품평회), 황금 밀(5단계 주문). **Lv9·10 에는 아직 새 작물이 없다.**
- **특수 규칙** — 작물표의 표시만 보고 규칙이 걸린다

  | 표시 | 작물 | 규칙 |
  |---|---|---|
  | `thirsty` | 쌀 | 하루 굶으면 🍂, 사흘이면 💀 (보통은 이틀 · 나흘) |
  | `tall` | 옥수수 · 해바라기 | 이웃에 그늘 — 버섯·잎 +10%, 열매·박 −5% |
  | `shadeNeed` | 향송이 | 키 큰 이웃이 없으면 성장 ×0.5 |
  | `perennial` | 아스파라거스 · 찻잎 · 라벤더 | 윤작·연작을 안 따진다 |
  | `aura` | 라벤더 | 이웃 품질 +5(3b 가 읽는다) |
  | `spread` | 박하 | 밭을 거둘 때마다 같은 밭 빈 흙에 한 포기 번진다 |
  | `scream` | 비명 뿌리 | 이웃 −10%. 밭을 한 번 거둘 때마다 귀마개 하나, 없으면 체력 −5(1 은 남긴다). 판에 앉아 있으면 그 밭만 못 거둔다 |
  | `flee` | 도망가는 버섯갓 | 익은 날 밤까지 안 거두면 같은 밭 빈 흙으로(해시로 고정) 옮겨 이튿날 하루 더 익어 있다. 빈 흙이 없으면 사라진다 |
  | `seedOnly` | 희귀 셋 | 씨앗을 못 산다. 개간 전리품(보통 2% · 완벽 5%, 계정당 하루 1)으로 주운 **주머니 씨앗**으로 레벨 없이 심는다 |

- **주머니**(`pouches`)는 `farms.json` 에 계정 기준으로 둔다 — 줍기(개간)·쓰기(심기)가 칸 바꾸기와 같은 파일, 한 번의 쓰기다.
- **귀마개**(`earPlug`) — 상점 🌾 농사 진열대 50골드, 1회용.
- **심기 셀렉트는 쪽을 넘긴다** — 24개씩 + "▶ 다른 작물". 희귀는 주머니에 있을 때만 맨 앞에.

#### 재수확 작물의 균형 (3c에서 정함)

오래 두고 보면 재수확 작물은 두 번째부터 씨앗값이 없어 **한 번 거두는 작물의 하루당 3.2배**가 남았다(2a 부터 있던 토마토·딸기 포함). 정한 것:

1. **재수확 간격은 처음 성장일보다 짧게 둔다. 대신 처음 성장일을 재수확 간격의 두 배 이상으로** — 오이·상추·산딸기 2→4, 대파·가지·로즈마리·바질·블루베리 3→4, 파프리카 4→6, 찻잎·라벤더 5→6. 처음은 오래, 그다음은 빨리.
2. **재수확 작물은 좋은 선택으로 남아도 된다.** 수익은 건드리지 않는다.
3. 대신 **재수확으로 거둔 칸의 농장 경험치는 절반**(0.5). 재수확 작물을 잘 쓰면 Lv10 이 33~37일로 목표보다 빨랐다 → 이웃+먹으며 48일, 먹으며 57일(`simulate-farm`, 60일 × 12판).

`simulate-farm` 은 작물을 **오래 두고 본 하루당 가치**(재수확이면 `파는 값 / 재수확 간격`)로 고른다. 처음 한 번의 보장 이익만 보면 재수확 작물을 과소평가해 이 불균형을 못 본다.

### 채소 (veg)

| Lv | 작물 | key | 기본가 | 계열 | 성장 | 재 | 제철 | 비고 |
|---|---|---|---|---|---|---|---|---|
| 1 | 감자 | potato | 2 | 뿌리 | 2 | | 봄·가 | |
| 1 | 당근 | carrot | 2 | 뿌리 | 3 | | 봄·가 | 느린 편 +1 |
| 1 | 시금치 | spinach | 3 | 잎 | 2 | | 가·겨 | 빠른 편 −1 |
| 1 | 오이 | cucumber | 2 | 박 | 2 | 2 | 여 | |
| 1 | 무 | radish (신규) | 3 | 뿌리 | 3 | | 가·겨 | 거대 |
| 1 | 상추 | lettuce (신규) | 2 | 잎 | 2 | 2 | 봄·가 | |
| 2 | 양파 | onion | 2 | 파속 | 3 | | 봄 | +1 |
| 2 | 마늘 | garlic | 3 | 파속 | 4 | | 가·겨 | +1, 서리에 강함 |
| 2 | 대파 | greenOnion | 3 | 파속 | 3 | 2 | 사계절 | |
| 2 | 양배추 | cabbage | 3 | 잎 | 3 | | 봄·가 | 거대 |
| 2 | 토마토 | tomato | 4 | 열매 | 4 | 2 | 여 | |
| 2 | 깻잎 | perilla (신규) | 2 | 허브 | 2 | 1 | 여 | |
| 3 | 가지 | eggplant | 3 | 열매 | 3 | 2 | 여 | |
| 3 | 고구마 | sweetPotato | 3 | 뿌리 | 4 | | 여·가 | +1 |
| 3 | 옥수수 | corn | 3 | 곡물 | 4 | | 여 | +1, 키 큼, 그늘 |
| 3 | 고추 | chili (신규) | 4 | 열매 | 4 | 2 | 여 | |
| 3 | 순무 | turnip (신규) | 3 | 뿌리 | 3 | | 겨 | 서리에 강함 |
| 4 | 배추 | napaCabbage | 3 | 잎 | 4 | | 가 | +1, 거대 |
| 4 | 브로콜리 | broccoli | 4 | 잎 | 4 | | 가·겨 | |
| 4 | 파프리카 | paprika | 5 | 열매 | 4 | 3 | 여 | |
| 4 | 비트 | beet (신규) | 4 | 뿌리 | 4 | | 가 | |
| 5 | 늙은 호박 | pumpkin | 8 | 박 | 6 | | 가 | +1, 거대 |
| 5 | 토란 | taro (신규) | 5 | 뿌리 | 5 | | 가 | +1 |
| 6 | 아스파라거스 | asparagus | 6 | 다년 | 7 | 3 | 봄 | +2, 다년생 |
| 9 | 용의 고추 | dragonChili (신규) | 15 | 열매 | 10 | 4 | 여 | 폭염에 ×2, 날로 먹으면 아프다 |

### 곡물·콩 (grain)

| Lv | 작물 | key | 기본가 | 계열 | 성장 | 재 | 제철 | 비고 |
|---|---|---|---|---|---|---|---|---|
| 1 | 밀 | wheat | 2 | 곡물 | 3 | | 봄·가 | +1 |
| 1 | 콩 | soybean | 3 | 콩 | 3 | | 여 | |
| 2 | 완두콩 | pea (신규) | 3 | 콩 | 3 | | 봄 | |
| 3 | 귀리 | oats | 3 | 곡물 | 3 | | 봄·가 | |
| 3 | 보리 | barley | 3 | 곡물 | 3 | | 겨·봄 | 서리에 강함 |
| 3 | 강낭콩 | kidneyBean (신규) | 3 | 콩 | 3 | | 여 | |
| 4 | 메밀 | buckwheat | 4 | 곡물 | 3 | | 여·가 | −1 |
| 5 | 쌀 | rice | 6 | 곡물 | 5 | | 여 | 물 욕심, 비에 ×1.2 |
| 5 | 해바라기 | sunflower (신규) | 4 | 곡물 | 5 | | 여 | +1, 키 큼, 그늘 |
| — | 황금 밀 | goldenWheat (신규) | 18 | 곡물 | 10 | | 가 | 희귀 씨앗으로만 얻음 (주문 보상) |

### 과일 (fruit)

| Lv | 작물 | key | 기본가 | 계열 | 성장 | 재 | 제철 | 비고 |
|---|---|---|---|---|---|---|---|---|
| 4 | 딸기 | strawberry | 5 | 베리 | 4 | 2 | 봄 | |
| 5 | 산딸기 | raspberry | 2 | 베리 | 2 | 2 | 여 | |
| 5 | 블루베리 | blueberry | 3 | 베리 | 3 | 2 | 여 | 다년생 |
| 5 | 사과 | redApple | 5 | 과수 | 10 | 3 | 가 | 나무 (밭 1개 통째), 묘목 10 |
| 5 | 레몬 | lemon | 4 | 과수 | 10 | 3 | 겨 | 나무, 묘목 8 |
| 6 | 포도 | grape | 4 | 과수 | 8 | 3 | 가 | 덩굴나무, 묘목 8 |
| 6 | 복숭아 | peach | 6 | 과수 | 12 | 4 | 여 | 나무, 묘목 12 |
| 6 | 참외 | koreanMelon (신규) | 6 | 박 | 5 | | 여 | |
| 7 | 수박 | watermelon | 12 | 박 | 7 | | 여 | 거대 |
| 7 | 멜론 | melon (신규) | 14 | 박 | 7 | | 여 | 거대 |
| 7 | 배 | pear (신규) | 7 | 과수 | 12 | 4 | 가 | 나무, 묘목 14 |
| — | 파수꾼 베리 | keeperBerry | 30 | 베리 | 10 | 3 | 가 | 희귀 씨앗으로만 얻음 (개간) |

### 향신료·허브 (spice)

| Lv | 작물 | key | 기본가 | 계열 | 성장 | 재 | 제철 | 비고 |
|---|---|---|---|---|---|---|---|---|
| 4 | 박하 | mint | 3 | 허브 | 3 | 1 | 봄·여 | **퍼짐:** 수확할 때 같은 밭 빈 칸에 한 포기 번짐 |
| 4 | 로즈마리 | rosemary | 3 | 허브 | 3 | 2 | 사계절 | |
| 4 | 바질 | basil (신규) | 3 | 허브 | 3 | 2 | 여 | |
| 5 | 참깨 | sesame (신규) | 4 | 곡물 | 4 | | 여 | |
| 6 | 생강 | ginger | 4 | 뿌리 | 5 | | 가 | +1 |
| 6 | 찻잎 | teaLeaf | 6 | 다년 | 5 | 3 | 봄 | 다년생 |
| 6 | 라벤더 | lavender (신규) | 6 | 허브 | 5 | 3 | 여 | 다년생, 이웃 품질 +5 추가 |
| 8 | 사프란 | saffron | 60 | 허브 | 14 | | 가 | 귀한 향신료 |
| 10 | 월광초 | moonHerb (신규) | 20 | 허브 | 10 | | 사계절 | 맑은 날에만 자람 |
| 10 | 인삼 | ginseng (신규) | 40 | 뿌리 | 14 | | 사계절 | 다 자란 뒤 수확을 미루면 **과숙 대신 무게가 늘어난다** (최대 +14일, 품평회의 꽃) |

### 버섯·괴식 (veg)

| Lv | 작물 | key | 기본가 | 성장 | 비고 |
|---|---|---|---|---|---|
| 8 | 향송이 | pineMushroom | 25 | 10 | 그늘 이웃이 필요하다 |
| — | 비명 뿌리 | screamRoot | 14 | 7 | 희귀 씨앗. 수확할 때 귀마개가 없으면 HP −5. 이웃 성장 −10% |
| — | 도망가는 버섯갓 | walkingCap | 8 | 5 | 희귀 씨앗. 다 자란 날 수확하지 않으면 도망간다 |

### 심을 수 없는 것

- 잡초로만 나는 것: 민들레(dandelion)
- 키울 수 없는 것: 콩나물(beanSprout), 독버섯류, 도토리, 이끼, 가게 가공품(밀가루 등), 월계수 잎(나무 잎)

### 새로 필요한 아이템 키

| 종류 | 개수 | 내용 |
|---|---|---|
| 신규 작물 | 약 22 | 위 표의 (신규) |
| 품질 변형 | 작물 수 × 3 | `${key}S1`/`S2`/`S3`. 명부에 **코드로 생성**한다. 이름은 "당근 ★", "당근 ★★", "당근 ★★★" |
| 거대 작물 | 6 | `giantRadish`, `giantCabbage`, `giantNapaCabbage`, `giantPumpkin`, `giantWatermelon`, `giantMelon` (3b에서 확정) |
| 거름 | 3 | `compost`, `fertilizer`, `goldFertilizer` |
| 기타 | 2 | `oldCoin`, `earPlug`(귀마개) |

주의할 점:

- 새 키는 모두 `shop: false` 라 **상점 진열대 25개 한도에 걸리지 않는다.**
- 팔기 목록은 이미 쪽으로 나뉘어 있다.
- `/아이템 목록` 이 길어진다. 품질 변형은 목록에서 "★ 작물" 한 줄로 묶어 보여주는 것을 검토한다.
- **key 는 한번 정하면 바꾸지 않는다.**

---

## 13. 품질 판정 (B)

### 3b 구현에서 정한 것 — 원본은 `server/src/farm/quality.js`

- **점수 = 토질★×8 + 안 시듦 15 + 궁합(±10 에서 자름) + 윤작 +10 / 연작 −10 + 제철 +10 + 과숙 −15 + 작물 등급(큰 −5 · 귀한 −10 · 아주 귀한 −15) + 거대 실패 위로 +10 + 난수 0~40.** 궁합 몫은 이웃·세 자매·라벤더 향을 더해 ±10 에서 자르고, 윤작·연작 몫은 따로다(`affinity.js`).
- **제철은 4단계 전까지 늘 +10**(모든 작물이 제철). 4단계에서 제철이 아니면 −20 이 된다.
- **★ 변형 아이템**은 봇 명부가 `FARM_CROPS`(서버 작물표와 같아야 함 — 검사가 본다)로 **코드로 만든다**: `carrotS1`~`S3`, 이름 "당근 ★", 값 ×1.2·×1.5·×2(올림), 먹으면 원래 작물과 같다. 51 × 3 = 153개. `/아이템` 목록에서는 빼고 원래 작물 카드에 "🌾 밭에서 키우면 ★ 3골드 · …" 로 적는다. **키 규칙은 바꾸지 않는다.**
- **요리 가짓수 보너스**는 ★ 변형을 원래 작물로 센다 — 당근과 당근★ 을 두 가지로 치면 보너스를 부풀릴 수 있다.
- **품질 경험치는 0.** 기획서의 ★ +1 · ★★ +2 · ★★★ +4 면 Lv10 이 25~28일, 절반이어도 31~35일로 목표(42~56일)를 한참 앞질렀다. 0 이면 이웃+먹으며 48일 · 먹으며 57일. 품질은 값과 도감으로만 보상한다.
- **품질 분포**(`simulate-farm` 60일): 궁합을 보고 가꾸면 보통 10% · ★ 51% · ★★ 33% · ★★★ 5%. 늘 같은 작물을 이어 심으면 63% · 37% · 0% · 0%.
- **거대 작물**은 Lv7 부터, 한 밭 아홉 칸이 같은 작물 · 전부 익음 · 한 번도 안 시듦일 때 수확에서 `10% + 토질★×4%`. 대왕 작물 키는 `giant` + 작물 키(`giantNapaCabbage`), 값 ×45, 경험치 +30. 아홉 칸을 **같은 날 심어 같이 익혀야** 하므로 일부러 노리는 목표다(시뮬레이션은 노리지 않아 0번).
- **도감**(`book`, farms.json 계정 기준)은 수확과 같은 쓰기로 적는다 — 작물마다 거둔 포기 · 최고 ★ · 대왕 수. `/농장 도감` 은 레벨 순 51줄, 안 키운 것은 ❔(희귀는 이름도 숨김), 수집률.

### 품질 점수

수확할 때 **칸마다** 굴린다.

```
점수 = 토질(★×8)
     + 무결석(심은 뒤 한 번도 안 시듦) +15
     + 궁합 −10~+10 · 윤작 +10 / 연작 −10
     + 제철 +10 / 제철 아님 −20
     + 날씨(무지개 +10 · 폭우 뿌리 −10)
     + 비료(황금 비료) +15
     + 과숙 −15
     + 작물 등급(큰 −5 · 귀한 −10 / −15)
     + 난수 0~40
```

| 점수 | 품질 |
|---|---|
| 49 이하 | 보통 (기존 키) |
| 50~74 | ★ 상품 |
| 75~94 | ★★ 특품 |
| 95 이상 | ★★★ 명품 |

- 시든 적이 있으면 상한이 ★ 이다.
- 갓 등록한 ★1 밭은 최고 73점이라 ★ 까지만 나온다.
- ★5 밭에 조건을 모두 갖추면 ★★ 가 확실하고, 운이 좋으면 ★★★ 이다. 이것이 성장의 체감이다.

### 수확량

칸당 수확량은 토질과 작물 등급으로 정한다(§4 「작물 등급」). 비싼 작물은 덜 나온다. 품질은 칸 단위로 정해져서 그 칸의 수확물 전부에 적용된다.

### 거대 작물

- 조건:
  - 거대가 되는 작물이다 (무, 양배추, 배추, 호박, 수박, 멜론)
  - 한 밭 9칸이 **전부 같은 작물**이고 **한 번도 시든 적이 없다**
  - 9칸이 **다 자란 상태**이다
  - 농장 Lv7 이상이다
- 수확할 때 `10% + 토질★ × 4%` 확률로 9칸이 **대왕 작물 1개**로 합쳐진다.
- 무게를 굴려 품평회에 기록한다. 도감에도 따로 표시한다.
- 실패하면 평소처럼 9칸을 수확하고 품질 +10 을 준다. 헛수고가 되지 않게 한다.

### 경제 목표 (시뮬레이션으로 검증)

- Lv1 농장의 하루 순수익은 **5~15골드** 정도로, 아주 소소하게 시작한다. 초반의 보상은 돈보다 레벨, 토질, 밭 해금이다.
  - 근거: 칸당 하루 0.5~0.9골드 × 열린 칸 수
- 만렙 농장의 하루 평균 순수익(판매 기준)은 **300~500골드**를 목표로 한다. ★5 칸당 하루 4~8골드 × 81칸이면 이 범위에 든다. 주문 보상은 따로이다.
  - 출첵(1000까지 채움)과 낚시 수익에 견주어 맞춘다.
- 골드를 쓰는 곳: 씨앗, 묘목, 설비, 곡괭이, 비료.
- `bot/scripts/simulate-farm.mjs` 로 성실한 플레이어, 가끔 하는 플레이어, 방치한 플레이어를 30일씩 돌려서 레벨 속도와 골드 증감을 확인한다.

---

## 14. 기술 설계

### 저장소

- 새 파일 `farms.json` 에 저장한다. `accounts.json` 과 나눈다.
- 서버 `services/farmStore.js` 가 담당한다. 기존 store 와 같이 한 번에 읽고, 고치고, tmp 파일에 쓴 뒤 rename 한다.
- **주인은 `farms[*].owner` 에만 적는다.** 아래 초안의 `owners` 색인은 만들지 않았다 — 두 곳에 적으면 언젠가 어긋난다.
- 1단계에 실제로 있는 필드: `channelId, guildId, owner, createdAt, lastTickDay, water: { day, by }, plots[].{ open, crop, cells }`, 칸은 `{ t: 'soil' }` · `{ t: 'plant', g, thirst, scar, ripeDay, planted }` · `{ t: 'dead', why }`. 나머지는 단계마다 더한다. 원본 설명은 `server/src/farm/rules.js` 머리말.

```js
{
  farms: {
    [channelId]: {
      channelId, guildId, owner, createdAt,
      level, xp, workers: [userId],
      lastTickDay: 'YYYY-MM-DD',
      water: { day, by: [userId], count },     // 오늘 물 준 기록
      equip: { rainBarrel, drain, sprinklerUsedWeek, covers: [plot], stakes: [plot] },
      plots: [ // 9개, index 0~8 = 키패드 1~9
        { open, crop, soilXp, history: ['뿌리','콩'], fert,
          cells: [ // 9개
            { t: 'rock'|'boulder'|'weed'|'soil'|'plant'|'dead',
              grain?,            // 바위의 결 위치 (1~5, 봇에 보내지 않음)
              cracked?,          // 금 간 바위
              growth, thirst, scar, ripeDay, regrowN }
          ] }
      ],
      week: { key, bestWeight, starCount, orders },  // 품평회 집계
      personalOrders: [...]
    }
  },
  owners:    { [userId]: channelId },
  cooldowns: { [userId]: 'YYYY-MM-DD' },          // 재등록 가능일
  pouches:   { [userId]: { [seedKey]: n } },      // 희귀 씨앗
  helpers:   { [`${userId}:${channelId}`]: days }, // 이웃 호감
  daily:     { [userId]: { day, helps, clearPower, rareDrops } },
  board:     { day, orders: [...] },               // 마을 주문
  fair:      { week, results }                     // 지난주 정산
}
```

### 하루치 몰아서 계산 (크론 없음)

모든 요청은 맨 먼저 `tick(farm, today)` 를 돈다.
`lastTickDay + 1` 부터 **어제까지** 하루씩:

1. 그날의 날씨와 계절을 **해시로 다시 계산한다** (저장하지 않는다).
2. 물을 줬거나 비가 왔으면: 물을 받은 칸은 이미 물 줄 때 성장이 반영돼 있다. 비가 온 날은 여기서 성장을 더한다.
3. 물을 안 줬으면: `thirst++` 하고 시듦과 죽음을 판정한다. 스프링클러가 있으면 주 1회 면제한다.
4. 서리와 폭풍 피해를 반영한다.
5. 다 자란 칸은 과숙, 썩음, 도망을 판정한다.
6. 빈 칸에 잡초가 날지 판정한다.

마지막으로 `lastTickDay = 어제` 로 바꾼다. 오늘의 비는 그날 첫 조회에서 바로 반영한다.

### 라우트

모두 `requireBot` 뒤에 둔다. 확인과 쓰기가 한 번에 일어나는 규칙은 서버가 판정한다.

| 메서드 | 경로 | |
|---|---|---|
| GET | `/farms/:channelId` | tick 후 화면용 데이터 (결 위치는 빼고 보낸다) |
| GET | `/farms/by-owner/:userId` | 내 농장 |
| POST | `/farms/register` | 등록 (주인, 1인 1농장, 쿨다운) |
| POST | `/farms/abandon` | 폐농 (무르기, 쿨다운) |
| POST | `/farms/water` | 물주기 (하루 1회 또는 폭염 2회, 이웃 보상, 호감) |
| POST | `/farms/plant` | 심기 (골드 차감과 칸 갱신을 한 번에) |
| POST | `/farms/harvest` | 수확 (품질·거대 판정, 계정 `items` 지급, 경험치, 품평회 집계) |
| POST | `/farms/clear` | 개간 (돌, 바위 한 번 휘두르기, 기력, 전리품) |
| POST | `/farms/fertilize` | 거름 |
| POST | `/farms/equip` | 설비 구매 |
| GET/POST | `/farms/board`, `/farms/deliver` | 주문 조회와 납품 |
| GET | `/farms/fair` | 품평회 (새 주면 정산) |
| POST | `/farms/workers` | 일꾼 관리 |

- 수확, 심기, 개간은 **계정 쓰기(골드와 items)와 농장 쓰기가 짝**이다. 두 파일이라 한 번에 못 쓴다. 1단계에서 정한 규칙:
  1. 두 파일을 **다 읽고**, 규칙과 잔액 검사를 **다 끝낸 뒤** 쓴다. 거절은 쓰기 전에 난다.
  2. 쓰는 순서는 **"잃을 수는 있어도 복제되지는 않게"** 한다. 심기는 계정(골드) → 농장, 수확은 농장 → 계정. 두 쓰기 사이에 서버가 죽으면 씨앗값이나 그 수확만 잃는다.
  3. 계정은 `accountController` 가 내보낸 `load`·`publicView` 로 읽고 그린다. delta 적용 함수를 떼어 내는 리팩터링은 하지 않았다.
- **조회(GET)는 셈만 하고 쓰지 않는다.** 1단계의 하루치 셈에는 무작위가 없어 몇 번 셈해도 같다. 2단계에서 무작위(잡초 등)가 들어오면 날짜를 시드로 한 난수를 써서 이 성질을 지킨다.
- **못 한 것은 200 `{ ok: false, reason }`** 이다(`/claim` 과 같은 규약). 호출이 틀린 것(모양이 틀린 id·칸·작물)만 400.
- 씨앗값은 **가진 골드 안에서만** 낸다. 계정의 −1000 하한까지 빌려 심게 하지 않는다.

### 봇

| 파일 | 역할 |
|---|---|
| `bot/src/commands/farm.js` | `/농장` 서브커맨드, `componentPrefix: 'farm'` |
| `server/src/farm/crops.js` | **작물표의 원본은 서버에 둔다**(§16 1번 결정). 서버와 봇이 Railway 에서 따로 배포돼 실행 중에는 파일을 같이 못 읽는다. 봇은 `GET /api/farms/crops` 를 10분 캐시한다. 서버는 아이템 값을 몰라 `price`·`name` 을 옮겨 적었고, 어긋나면 `bot/scripts/check-farm.mjs` 가 잡는다 |
| `bot/src/farm/render.js` | 9×9 이모지 격자와 밭 요약 줄 |
| `bot/src/farm/weather.js` | 날씨와 계절 해시. 서버와 같은 식을 쓴다 (`pickOfDay` 규약과 같다) |
| `bot/src/casino/items.js` | 신규 작물, ★ 변형 생성, 거대 작물, 거름 추가 |
| `bot/src/casino/titles.js` | 「대농」, 「품앗이」, 「품평회 우승」 |
| `bot/scripts/check-farm.mjs` | 작물표의 무결성(키가 명부에 있는지, 성장일 규칙, 제철) 검사 |
| `bot/scripts/simulate-farm.mjs` | 경제 시뮬레이션 |

### 새 계정 카운터

농장 카운터는 **서버의 농장 라우트가 계정 `stats` 에 직접 쓴다.** 봇이 `/deltas` 의 `bump` 로 보내지 않으므로 `BUMP_KEYS` 에 넣을 필요가 없다.
1단계: `farmWater`(물 준 횟수), `farmHelp`(남의 농장에 물 준 횟수), `farmHarvest`(거둔 칸 수).
이후 단계: `farmClear`, `farmPerfect`, `farmGiant`, `farmOrders`, `fairWins`, `farmStar3`

### 배포 순서

단계마다 **서버를 먼저 배포**하고, 그다음 봇을 배포한 뒤, 명령어 구조가 바뀌었으면 `npm run register` 를 실행한다.
main 에 push 하면 둘 다 재배포되고 진행 중인 게임이 끊긴다.

---

## 15. 구현 로드맵

| 단계 | 내용 | 비고 |
|---|---|---|
| **0 상점 값 분리** ✅ | `items.js` 에 `buyPrice(item)`(재료만 `item.buy ?? item.price × 2`, 나머지는 `price`), 상점 사기(`shop.js`)와 `/아이템` 표시("사기 X · 팔기 Y")만 바꾼다. 파는 값을 쓰는 곳(요리, 던전, 홀덤, 프로필)은 그대로 둔다 | **1단계와 같은 PR로 낸다.** 이 부분은 봇만 바뀐다 |
| **1 MVP** ✅ | 저장소와 라우트 뼈대, 등록·폐농(무르기, 쿨다운)·보기·내농장, 심기·물주기·수확, 작물별 성장일, 시듦·죽음·과숙, 5번 밭만, 보통 품질만, **Lv1 작물 8종**(감자·당근·시금치·오이·무·상추·밀·콩). 레벨이 없는 단계라 Lv2 작물은 레벨과 함께 연다 | 구현됨. 검사: `server` 의 `npm run check-farm`, `bot` 의 `npm run check-farm` |
| **2a 땅과 레벨** ✅ | 돌·바위·잡초, 결 찾기 미니게임, 기력(계정 기준), 전리품, 토질★(성장·수확량), 농장 레벨과 밭 해금, Lv2~5 작물 28종, **체력으로 주는 물** | 구현됨. `simulate-farm` 도입 |
| **2b 거름과 곡괭이** ✅ | 퇴비(조각 셋 → 하나, 작물 다섯 → 하나)·비료(상점 🌾 농사 진열대 30골드, 하루 한도), 철·미스릴 곡괭이(전리품 쪽) | 구현됨 |
| **확장 (시기 미정)** | 3×3 바깥으로 밭을 넓힌다. 새 땅은 돌투성이라 개간·곡괭이가 끝까지 할 일로 남는다 | 기획 필요 |
| **3a 궁합·윤작** ✅ | 계열, 이웃 밭 궁합(±30%), 세 자매, 윤작과 연작(이어 심기 포함), 궁합 미리보기와 🤝⚔️ 표시 | 구현됨 |
| **3c 새 작물** ✅ | Lv4~8 특수 작물 12종, 희귀 씨앗 셋과 주머니, 귀마개, 심기 쪽 넘김. 재수확 균형(처음 성장일 ×2 · 재수확 경험치 절반) | 구현됨 — §12 「3c 구현에서 정한 것」 |
| **3b 품질·거대·도감** ✅ | 품질 점수, ★ 변형 키 153(코드로 생성, 목록에선 묶음), 대왕 작물 6, `/농장 도감`. 품질 경험치 0 | 구현됨 — §13 「3b 구현에서 정한 것」 |
| **4 날씨 (C)** | 계절, 날씨 해시, 내일 예보, 날씨 효과, 설비 | |
| **5 주문 (F)** | 마을 주문과 개인 의뢰, 납품, NPC 웹훅 문구 | |
| **6 함께 (G)** | 이웃 보상과 호감, 일꾼, 주간 품평회, 칭호 | |
| **7 오늘의 씨앗 가게** | `/농장 씨앗가게` — 아래 참고 | 1~6단계가 안정된 뒤 |

### 7단계 — 오늘의 씨앗 가게 (나중에 추가)

기본 씨앗은 지금처럼 심을 때 산다. 여기에 **날마다 바뀌는 가게**를 얹어, 매일 들어와 볼 이유를 하나 더 만든다.

- 진열은 KST 날짜 해시로 정한다. 날씨와 같은 방식이라 모든 농장이 같은 가게를 본다.
- 진열 칸은 3~4개다.

| 칸 | 내용 | 사는 방식 |
|---|---|---|
| 🏷️ 특가 | 작물 1~2종의 **보장 이익이 오늘만 두 배**다(씨앗값 = 파는 값 − 2m). 할인율로 깎지 않는 이유는, 비율 할인은 비싼 작물일수록 이득이 커지기 때문이다 | 사는 행동이 따로 없다. 오늘 그 작물을 심으면 자동으로 적용된다 |
| 🌸 철 이른 씨앗 | 다음 계절 제철 작물 1종을 한 단계 낮은 레벨에서 미리 심을 수 있다. 대신 보장 이익이 1로 줄어든다(씨앗값 = 파는 값 − 1). 씨앗값을 배수로 올리면 파는 값을 넘어 손해가 날 수 있어서 이렇게 한다 | 심을 때 적용된다 |
| 🎒 떠돌이 씨앗 | 희귀 씨앗 1종. 비싸고(초안 300~800골드), **계정당 하루 1개** | 씨앗 주머니에 들어간다 |

- **떠돌이 씨앗이 추가되면 희귀 씨앗을 골드로 살 수 있게 된다.** 그러면 §10의 "희귀 씨앗은 돈으로 못 산다"는 문구를 "하루 1개, 비싸게만"으로 고친다.
  - 개간과 주문 보상에서 나오는 희귀 씨앗의 가치가 떨어지지 않도록, 가게에는 황금 밀을 올리지 않는다(주문 보상 전용으로 남긴다).
- 씨앗 아이템 키는 여전히 만들지 않는다. 주머니 씨앗을 이웃에게 선물하는 기능은 이 단계에서 함께 검토한다(G와 연결).

단계마다 한 PR로 낸다. 서버 → 봇 → register 순서로 배포한다. 새 필드는 서버의 `normalize` 와 `publicView` 에도 넣는다.

---

## 16. 남은 결정

1. ~~**작물표의 위치**~~ → **서버**에 두기로 했다(§14 봇 표).
2. **계절 길이:** 1주가 너무 빠른지 (2주로 늘릴 여지).
3. ~~**품질 변형 키**~~ → ★·★★·★★★ 세 단계, 코드로 생성하고 목록에서 묶는다(3b).
4. **품평회 상금과 주문 보상의 재원:** 모두 새로 만들어지는 골드로 할지, NPC 계정에서 나가게 할지.
