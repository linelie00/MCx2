# Railway/Nixpacks 빌드 설정
#
# ffmpeg 는 여기서 깔지 않아도 된다 — ffmpeg-static 패키지로 들고 오기 때문이다.
# Railway 에서 이 파일의 aptPkgs 가 적용되지 않아(빌더 설정에 좌우된다) 재생이 전부
# ENOENT 로 실패한 적이 있어서 패키지 방식으로 바꿨다.
#
# 그래도 시스템 ffmpeg 가 있으면 그쪽을 먼저 쓰므로, Nixpacks 를 쓰는 환경에서는
# 79MB 짜리 다운로드 대신 이게 쓰인다.
[phases.setup]
aptPkgs = ["ffmpeg"]
